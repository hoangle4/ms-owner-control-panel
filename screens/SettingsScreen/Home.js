import React from 'react';
import PropTypes from 'prop-types';
import { View, Text } from 'react-native';
const Home = (props) => {
	return (
		<View>
			<Text>Home</Text>
		</View>
	);
};

Home.propTypes = {};

export default Home;
